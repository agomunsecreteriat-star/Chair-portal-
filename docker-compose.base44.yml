services:
  web:
    image: node:22
    working_dir: /app
    volumes:
      - .:/app
    command: sh -c "npx --yes live-server --host=0.0.0.0 --port=3000 --no-browser ."
    ports:
      - "3000:3000"
